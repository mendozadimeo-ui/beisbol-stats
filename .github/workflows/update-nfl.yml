name: Update NFL odds snapshot

on:
  schedule:
    - cron: "30 */2 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-nfl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Fetch odds snapshot
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: node scripts/fetch-nfl.mjs

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/nfl.json data/nfl-history.json
          git diff --cached --quiet || git commit -m "Actualizar cuotas de NFL [automatico]"
          git push
